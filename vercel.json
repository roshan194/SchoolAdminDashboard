{
    "version": 2,
    "buildCommand": "vite build",
    "outputDirectory": "dist",
    "installCommand": "npm install",
    "routes": [
      {
        "src": "/api/(.*)",
        "dest": "/api/index.ts"
      }
    ],
    "env": {
      "NODE_ENV": "production"
    }
  }
  